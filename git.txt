what are the commands that we learned is :

cd
mkdir
git status
git init
git add .
git commit -m " "
git push origin main
git clone
git branch
git checkout (name)
git pull origin main
git branch <branch_name>
git checkout -b <branch_name>
git merge <branch_name>
